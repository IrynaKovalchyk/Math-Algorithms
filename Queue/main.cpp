#include <iostream>
#include "Queue.h"

int main() {
    Queue<int> queue (3);
    queue.push(1);
    queue.push(3);
    queue.push(5);
    queue.push(6);
    queue.pop();

    queue.print();
    return 0;
}

// Клас їжа в якого наслідується гарнір і напій в їжі чиста віртуальна функція спожити їжу, зчитати з файлу масив їжі(гарнір або напій)
// і вивести то дєрмо, використовувати перевантаження операторів
// функція з'їсти все(виводить що ти з'їв)